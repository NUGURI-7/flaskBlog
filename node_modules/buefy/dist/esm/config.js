import { c as config, s as setOptions } from './config-3Md_fpMO.js';
import { merge } from './helpers.js';
import 'vue';

var ConfigComponent = {
  getOptions() {
    return config;
  },
  setOptions(options) {
    setOptions(merge(config, options, true));
  }
};

export { ConfigComponent as default };
